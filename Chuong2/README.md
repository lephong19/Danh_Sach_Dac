-Trình bày / mô phỏng thuật toán sắp xếp:
+selectionSort: 
    // Mo phong :
     "Buoc i || Tim Thay min_pos || Hoán vị ai || Mảng sau khi hoán vị";

    // Dem phep so sanh:
     "Buoc i || số lần so sánh || Tìm thấy min_pos || Hoán vị";
     "Cong Thuc: n*(n-1)/2";

    // Y tuong
    "Voi tung luot o vong lap i thi ta deu chon moc so sanh o dau vong lap 
    va so sanh voi tung phan tu sau moc do , Neu tim Thay pt nhỏ hơn thì reset mốc đó , đi hết mảng và hoán vị ...";

    // Độ phức tạp thuật toán
    // So sanh do hieu qua cac thuat toan



    ===========================
    insertSort: đã sx: O(n) , Ngược thứ tự O(n*n)
    // Mô phỏng:
    "Buoc i || key || số lần so sánh || số lần dịch chuyển || Dãy sau khi dịch"

    // Dem phep so sanh:
    Lấy tổng của cột "số lần so sánh" ở bảng mô  phỏng


    ===========================
    bubbleSort: 
    // Mô phỏng: 
    Lượt i || Phạm vi j || Số lần so sánh || Mảng sau lượt